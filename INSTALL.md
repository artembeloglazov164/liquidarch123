# 📦 Инструкция по установке macOS Liquid Arch

## Требования

- 64-битный процессор (x86_64)
- Минимум 2 ГБ RAM (рекомендуется 4 ГБ+)
- 20 ГБ свободного места на диске
- UEFI или BIOS
- Интернет соединение (для обновлений)

## Сборка ISO образа

### 1. Подготовка

На существующей системе Arch Linux:

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/macos-liquid-arch.git
cd macos-liquid-arch

# Установите зависимости
sudo pacman -S archiso
```

### 2. Сборка

```bash
cd build
sudo ./build.sh
```

ISO образ будет создан в директории `out/`.

### 3. Запись на USB

```bash
# Найдите ваше USB устройство
lsblk

# Запишите ISO (замените /dev/sdX на ваше устройство)
sudo dd bs=4M if=out/macos-liquid-arch-*.iso of=/dev/sdX status=progress oflag=sync
```

## Установка

### 1. Загрузка Live ISO

- Вставьте USB и перезагрузите компьютер
- Войдите в Boot Menu (обычно F12, F2, Del или Esc)
- Выберите USB устройство
- Выберите "320kgpenguin (macOS Liquid Arch)" в меню GRUB

### 2. Вход в Live систему

Система автоматически войдет под пользователем **liveuser** без запроса пароля.

**Учетные данные Live ISO:**
- **Пользователь:** `liveuser` (без пароля)
- **Root:** `root` (без пароля)
- **Sudo:** работает без пароля для всех команд

### 3. Установщик Calamares

Calamares установщик должен запуститься автоматически после загрузки.

**Если установщик не запустился автоматически:**
```bash
sudo calamares
```

**Шаги установки:**

1. **Язык и регион** - выберите русский язык
2. **Клавиатура** - выберите раскладку (обычно Russian)
3. **Разметка диска** - выберите:
   - **Автоматическая** - Calamares сам разметит диск
   - **Ручная** - для dual boot с Windows или кастомной разметки
4. **Пользователь** - создайте учетную запись:
   - Имя пользователя (например: user)
   - Пароль (запомните его!)
   - Имя компьютера (например: macos-liquid-arch)
5. **Сводка** - проверьте все настройки
6. **Установка** - нажмите "Установить" и дождитесь завершения (10-20 минут)
7. **Завершение** - перезагрузите систему

### 4. Первый запуск

После перезагрузки:

1. Войдите в систему
2. Latte Dock запустится автоматически
3. Настройте систему под себя

## Настройка после установки

### Обновление системы

```bash
sudo pacman -Syu
```

### Установка AUR helper (yay)

```bash
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si
```

### Дополнительные темы

```bash
yay -S kvantum-theme-materia
yay -S papirus-icon-theme
yay -S apple-fonts
```

## Горячие клавиши

- `Meta` - Launchpad (меню приложений)
- `Meta + Q` - закрыть окно
- `Meta + W` - закрыть вкладку
- `Meta + T` - новый терминал
- `Meta + E` - Dolphin (файловый менеджер)
- `Meta + Tab` - переключение окон
- `Meta + Space` - поиск (KRunner)
- `Ctrl + →/←` - переключение рабочих столов
- `Ctrl + ↑` - Mission Control (все окна)

## Решение проблем

### Latte Dock не запускается

```bash
latte-dock &
```

### Нет звука

```bash
systemctl --user restart pipewire pipewire-pulse
```

### Проблемы с Wi-Fi

```bash
sudo systemctl restart NetworkManager
```

## Поддержка

- Документация: https://macos-liquid-arch.org/docs
- Форум: https://forum.macos-liquid-arch.org
- GitHub Issues: https://github.com/yourusername/macos-liquid-arch/issues
