# 🔧 Режимы сборки 320kgpenguin

## Два режима сборки

320kgpenguin поддерживает два режима сборки ISO:

### 1. LITE режим (по умолчанию)

**Описание:** AUR пакеты устанавливаются при первом запуске Live ISO

**Преимущества:**
- ✅ Быстрая сборка ISO (~20-30 минут)
- ✅ Экономия места на диске при сборке
- ✅ Подходит для GitHub Actions (ограничения по памяти)
- ✅ Меньший размер ISO

**Недостатки:**
- ❌ Требуется интернет при первом запуске Live ISO
- ❌ Первый запуск занимает ~10-15 минут (установка AUR пакетов)

**Когда использовать:**
- Сборка в GitHub Actions
- Ограниченные ресурсы (RAM, диск)
- Быстрое тестирование изменений

---

### 2. FULL режим

**Описание:** Все пакеты (включая AUR) устанавливаются во время сборки ISO

**Преимущества:**
- ✅ Готовая система сразу после загрузки
- ✅ Не требуется интернет при первом запуске
- ✅ Calamares доступен сразу
- ✅ Все темы и компоненты уже установлены

**Недостатки:**
- ❌ Долгая сборка ISO (~40-60 минут)
- ❌ Требует больше места на диске (~50 ГБ)
- ❌ Требует больше RAM (~8-16 ГБ)
- ❌ Не подходит для GitHub Actions (нехватка памяти)
- ❌ Больший размер ISO
- ❌ Может не работать в Docker с ограниченными ресурсами

**Когда использовать:**
- Локальная сборка в Docker
- Локальная сборка в VM
- Финальный релиз для пользователей
- Нет доступа к интернету при использовании Live ISO

---

## Переключение режимов

### Автоматическое переключение

**Linux/macOS:**
```bash
bash scripts/switch-build-mode.sh
```

**Windows:**
```powershell
.\scripts\switch-build-mode.ps1
```

### Ручное переключение

**На FULL режим:**
```bash
cp airootfs/root/customize_airootfs_full.sh airootfs/root/customize_airootfs.sh
```

**На LITE режим:**
```bash
git checkout -- airootfs/root/customize_airootfs.sh
```

---

## Сборка в разных режимах

### Docker

**LITE режим:**
```bash
# Linux/macOS
bash scripts/docker-build.sh
# Выберите: 1

# Windows
.\scripts\docker-build.ps1
# Выберите: 1
```

**FULL режим:**
```bash
# Linux/macOS
bash scripts/docker-build.sh
# Выберите: 2

# Windows
.\scripts\docker-build.ps1
# Выберите: 2
```

**Или напрямую через docker-compose:**
```bash
# LITE
docker compose run --rm archiso-builder

# FULL
docker compose run --rm archiso-builder-full
```

### Локальная сборка

1. Переключите режим:
```bash
bash scripts/switch-build-mode.sh
```

2. Соберите ISO:
```bash
cd build
sudo bash build.sh
```

### GitHub Actions

GitHub Actions всегда использует LITE режим (по умолчанию).

Для использования FULL режима в GitHub Actions:
1. Увеличьте лимиты (GitHub Pro/Team)
2. Или используйте self-hosted runners

---

## Сравнение

| Параметр | LITE | FULL |
|----------|------|------|
| Время сборки ISO | ~20-30 мин | ~40-60 мин |
| Размер ISO | ~3 ГБ | ~4 ГБ |
| Первый запуск | +10-15 мин | Сразу готов |
| Интернет при запуске | Требуется | Не требуется |
| Место при сборке | ~30 ГБ | ~50 ГБ |
| RAM при сборке | ~4 ГБ | ~8-16 ГБ |
| GitHub Actions | ✅ Работает | ❌ Не хватает памяти |
| Docker (ограниченные ресурсы) | ✅ Работает | ⚠️ Требует настройки |
| Docker (достаточно ресурсов) | ✅ Работает | ✅ Работает |
| VM | ✅ Работает | ✅ Работает |

---

## Что устанавливается из AUR

Оба режима устанавливают одинаковые пакеты, разница только во времени установки:

- **Latte Dock** - панель внизу с эффектом увеличения
- **Calamares** - установщик системы
- **MacSonoma KDE Theme** - тема KDE в стиле macOS
- **WhiteSur GTK Theme** - GTK тема
- **WhiteSur Icon Theme** - иконки
- **WhiteSur Cursors** - курсоры
- **Albert Launcher** - Spotlight для Linux
- **Lightly Qt** - стиль приложений

---

## Требования для FULL режима

### Docker Desktop

**Минимальные настройки:**
- Disk image size: **60 ГБ**
- Memory: **8 ГБ** (рекомендуется 16 ГБ)
- CPUs: **4** (рекомендуется 6-8)

**Как настроить:**
1. Docker Desktop → Settings (⚙️)
2. Resources → Advanced
3. Увеличьте параметры
4. Apply & Restart

### Виртуальная машина

**Минимальные настройки:**
- Диск: **60 ГБ**
- RAM: **8 ГБ** (рекомендуется 16 ГБ)
- CPU: **4 ядра** (рекомендуется 6-8)

### Реальный Arch Linux

**Минимальные требования:**
- Свободное место: **50 ГБ**
- RAM: **8 ГБ**
- Интернет: стабильное соединение

---

## Рекомендации

### Для разработки
Используйте **LITE режим** - быстрые итерации, меньше ожидания.

### Для тестирования
Используйте **LITE режим** - быстрее собрать и протестировать.

### Для релиза
Используйте **FULL режим** - пользователи получат готовую систему.

### Для GitHub Actions
Используйте **LITE режим** (по умолчанию) - единственный работающий вариант.

### Для Docker
Оба режима работают отлично, выбирайте по ситуации.

---

## FAQ

### Можно ли переключить режим после сборки?
Нет, режим определяется при сборке ISO. Нужно пересобрать ISO в другом режиме.

### Какой режим используется по умолчанию?
LITE режим - он установлен в `airootfs/root/customize_airootfs.sh`.

### Как узнать текущий режим?
```bash
grep "ПОЛНАЯ ВЕРСИЯ" airootfs/root/customize_airootfs.sh
# Если найдено - FULL режим
# Если не найдено - LITE режим
```

### Можно ли использовать FULL режим в GitHub Actions?
Технически да, но потребуется:
- GitHub Pro/Team (больше ресурсов)
- Или self-hosted runner с достаточными ресурсами

### Какой режим быстрее для пользователя?
- **LITE**: сборка 20 мин + первый запуск 15 мин = **35 минут**
- **FULL**: сборка 40 мин + первый запуск 0 мин = **40 минут**

Итого: LITE немного быстрее в общем времени.

### Какой режим лучше для оффлайн использования?
**FULL режим** - не требует интернета при первом запуске.

---

**Выбирайте режим в зависимости от ваших потребностей!**
