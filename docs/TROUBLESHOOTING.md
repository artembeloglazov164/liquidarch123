# 🔧 Решение проблем при сборке

## "not enough free disk space" в Docker

### Проблема
```
error: not enough free disk space
error: failed to commit transaction (not enough free disk space)
```

### Причина
Docker не хватает места на диске для установки AUR пакетов (особенно в FULL режиме).

### Решение

#### 1. Увеличьте размер диска Docker Desktop

**Windows/macOS:**
1. Откройте Docker Desktop
2. Settings (⚙️) → Resources → Advanced
3. **Disk image size**: увеличьте до **60 ГБ** (для FULL) или **40 ГБ** (для LITE)
4. **Memory**: увеличьте до **8 ГБ** (для FULL) или **4 ГБ** (для LITE)
5. **CPUs**: увеличьте до **4-6**
6. Apply & Restart

#### 2. Очистите Docker

```bash
# Остановите все контейнеры
docker stop $(docker ps -aq)

# Удалите все контейнеры
docker rm $(docker ps -aq)

# Очистите все (образы, volumes, кэш)
docker system prune -a --volumes

# Проверьте освобожденное место
docker system df
```

#### 3. Используйте LITE режим

LITE режим требует меньше ресурсов:

```bash
# Windows
.\scripts\docker-build.ps1
# Выберите: 1 (LITE)

# Linux/macOS
bash scripts/docker-build.sh
# Выберите: 1 (LITE)
```

#### 4. Соберите в виртуальной машине

Если Docker не помогает, используйте VM:
- См. [BUILD.md](BUILD.md)
- См. [ARCH-VM-QUICKSTART.md](ARCH-VM-QUICKSTART.md)

---

## "Permission denied" (Linux)

### Проблема
```
permission denied while trying to connect to the Docker daemon socket
```

### Решение

```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиньтесь или выполните
newgrp docker

# Проверьте
docker ps
```

---

## "Cannot connect to Docker daemon"

### Проблема
```
Cannot connect to the Docker daemon
```

### Решение

**Windows/macOS:**
- Запустите Docker Desktop

**Linux:**
```bash
# Запустите Docker daemon
sudo systemctl start docker

# Включите автозапуск
sudo systemctl enable docker

# Проверьте статус
sudo systemctl status docker
```

---

## Сборка зависает

### Проблема
Сборка останавливается и не продолжается.

### Решение

1. **Проверьте интернет соединение**
2. **Увеличьте timeout:**
   ```bash
   # В docker-compose.yml добавьте:
   environment:
     - MAKEPKG_TIMEOUT=3600
   ```
3. **Перезапустите сборку:**
   ```bash
   docker compose down
   docker compose run --rm archiso-builder
   ```

---

## Ошибки при установке AUR пакетов

### Проблема
```
error: failed retrieving file
error: target not found
```

### Решение

1. **Обновите зеркала Arch:**
   ```bash
   docker compose run --rm archiso-builder bash -c "sudo pacman -Syy"
   ```

2. **Попробуйте позже** - AUR пакеты могут быть временно недоступны

3. **Используйте LITE режим** - установка при первом запуске может быть надежнее

---

## ISO не создается

### Проблема
Сборка завершается, но ISO файла нет в `out/`.

### Решение

1. **Проверьте логи:**
   ```bash
   docker compose logs
   ```

2. **Проверьте права:**
   ```bash
   ls -la out/
   sudo chown -R $USER:$USER out/
   ```

3. **Пересоберите:**
   ```bash
   rm -rf build/work build/out
   docker compose run --rm archiso-builder
   ```

---

## Медленная сборка

### Проблема
Сборка занимает слишком много времени.

### Решение

1. **Увеличьте ресурсы Docker:**
   - CPUs: 6-8
   - Memory: 8-16 ГБ

2. **Используйте SSD** вместо HDD

3. **Проверьте скорость интернета** - загрузка пакетов может быть узким местом

4. **Используйте кэш:**
   ```bash
   # Не удаляйте volume pacman-cache
   docker volume ls
   ```

---

## Ошибки компиляции

### Проблема
```
error: failed to build package
makepkg: error
```

### Решение

1. **Проверьте зависимости:**
   ```bash
   docker compose run --rm archiso-builder bash
   # Внутри контейнера:
   pacman -Syu
   ```

2. **Обновите yay:**
   ```bash
   # В customize_airootfs.sh используется yay-bin
   # Это бинарная версия, не требующая компиляции
   ```

3. **Пропустите проблемный пакет:**
   - Отредактируйте `customize_airootfs_full.sh`
   - Добавьте `|| true` к проблемной команде

---

## GitHub Actions не работает

### Проблема
GitHub Actions падает с ошибкой нехватки места.

### Решение

**GitHub Actions поддерживает только LITE режим!**

Убедитесь, что используется `customize_airootfs.sh` (не `_full.sh`):

```bash
# Проверьте
cat airootfs/root/customize_airootfs.sh | grep "ПОЛНАЯ ВЕРСИЯ"
# Если найдено - переключите на LITE:
git checkout -- airootfs/root/customize_airootfs.sh
```

---

## Рекомендации

### Для успешной сборки FULL режима:

1. **Docker Desktop:**
   - Disk: 60 ГБ
   - RAM: 8-16 ГБ
   - CPU: 4-6 ядер

2. **Виртуальная машина:**
   - Disk: 60 ГБ
   - RAM: 8-16 ГБ
   - CPU: 4-6 ядер

3. **Стабильный интернет** - загрузка ~5-10 ГБ пакетов

### Если ресурсов недостаточно:

**Используйте LITE режим!**
- Быстрее
- Меньше ресурсов
- Тот же результат (пакеты установятся при первом запуске)

---

## Получить помощь

Если проблема не решена:

1. **Проверьте логи:**
   ```bash
   docker compose logs > build.log
   ```

2. **Создайте Issue:**
   - https://github.com/320kgpenguin/macos-liquid-arch/issues
   - Приложите логи
   - Укажите систему (Windows/macOS/Linux)
   - Укажите режим (LITE/FULL)

3. **Попробуйте альтернативный метод:**
   - Docker → VM
   - FULL → LITE
   - GitHub Actions

---

**Большинство проблем решается увеличением ресурсов Docker или переключением на LITE режим!**
