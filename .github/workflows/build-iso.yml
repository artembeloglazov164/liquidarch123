name: Build 320kgpenguin ISO

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
      options: --privileged
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Free up disk space
      run: |
        echo "=== Disk space before cleanup ==="
        df -h
        echo "=== Removing unnecessary packages ==="
        rm -rf /usr/share/dotnet || true
        rm -rf /opt/ghc || true
        rm -rf /usr/local/share/boost || true
        rm -rf /usr/local/lib/android || true
        rm -rf /opt/hostedtoolcache || true
        rm -rf /usr/share/swift || true
        rm -rf /usr/local/.ghcup || true
        rm -rf /usr/local/share/powershell || true
        rm -rf /usr/local/share/chromium || true
        rm -rf /usr/local/lib/node_modules || true
        docker system prune -af || true
        echo "=== Disk space after cleanup ==="
        df -h
    
    - name: Update system and install dependencies
      run: |
        pacman -Syu --noconfirm
        pacman -S --noconfirm archiso git base-devel
    
    - name: Build ISO
      run: |
        cd build
        chmod +x build.sh
        bash build.sh
    
    - name: Get ISO info
      id: iso
      run: |
        ISO_FILE=$(ls out/*.iso | head -n 1)
        ISO_NAME=$(basename $ISO_FILE)
        ISO_SIZE=$(du -h $ISO_FILE | cut -f1)
        echo "filename=$ISO_FILE" >> $GITHUB_OUTPUT
        echo "basename=$ISO_NAME" >> $GITHUB_OUTPUT
        echo "size=$ISO_SIZE" >> $GITHUB_OUTPUT
    
    - name: Upload ISO as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: 320kgpenguin-iso
        path: out/*.iso
        retention-days: 7
        compression-level: 0
    
    - name: Create Release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_number }}
        name: 320kgpenguin v${{ github.run_number }}
        body: |
          🐧 **320kgpenguin (macOS Liquid Arch)**
          
          Кастомный дистрибутив Arch Linux с дизайном macOS и эффектами жидкого стекла.
          
          ## 📦 Информация о сборке
          - **Файл**: ${{ steps.iso.outputs.basename }}
          - **Размер**: ${{ steps.iso.outputs.size }}
          - **Build**: #${{ github.run_number }}
          - **Commit**: ${{ github.sha }}
          
          ## 🍎 Особенности
          - ✨ KDE Plasma с жидким стеклом
          - 🎨 Latte Dock внизу с эффектом увеличения
          - 💿 Calamares установщик (встроен в ISO)
          - ⌨️ Горячие клавиши macOS
          - 🖥️ 4 рабочих стола
          - 🌊 Blur эффекты везде
          - 🎯 Темы macOS (fonts, wallpapers, plasmoids)
          
          ## 📥 Установка
          1. Скачайте ISO файл ниже
          2. Запишите на USB: `sudo dd bs=4M if=*.iso of=/dev/sdX status=progress`
          3. Загрузитесь с USB
          4. Calamares запустится автоматически
          5. Следуйте инструкциям установщика
          
          ## 👤 Учетные данные Live ISO
          - **Пользователь:** `liveuser` (без пароля, автологин)
          - **Root:** `root` (без пароля)
          - **Sudo:** работает без пароля
          
          ## 🚀 Быстрый старт
          См. [QUICKSTART.md](https://github.com/320kgpenguin/macos-liquid-arch/blob/main/QUICKSTART.md)
          
          ## 📚 Документация
          - [Установка](https://github.com/320kgpenguin/macos-liquid-arch/blob/main/INSTALL.md)
          - [FAQ](https://github.com/320kgpenguin/macos-liquid-arch/blob/main/docs/FAQ.md)
          - [Кастомизация](https://github.com/320kgpenguin/macos-liquid-arch/blob/main/CUSTOMIZATION.md)
          
          ---
          **Сделано с ❤️ для любителей macOS и Arch Linux**
        files: out/*.iso
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
