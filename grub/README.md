# GRUB Theme для macOS Arch

## Структура

Поместите файлы темы GRUB в папку `macOS-theme/`:

```
grub/
└── macOS-theme/
    ├── theme.txt          # Конфигурация темы (обязательно)
    ├── background.png     # Фоновое изображение
    ├── icons/             # Иконки для пунктов меню (опционально)
    │   ├── archlinux.png
    │   ├── windows.png
    │   └── ...
    └── fonts/             # Шрифты (опционально)
        └── ...
```

## Установка

Тема устанавливается автоматически при сборке ISO через скрипт:
```bash
/usr/local/bin/install-grub-theme.sh
```

Скрипт:
1. Копирует тему в `/boot/grub/themes/macos-arch/`
2. Обновляет `/etc/default/grub`
3. Устанавливает `GRUB_THEME="/boot/grub/themes/macos-arch/theme.txt"`

## Если темы нет

Если папка `macOS-theme/` пуста, скрипт создаст базовую тему с:
- Темным фоном (#1e1e2e)
- Розовым выделением пунктов меню
- Текстом "🍎 macOS Arch"

## Применение после установки системы

После установки системы на диск, GRUB нужно обновить:

```bash
sudo grub-mkconfig -o /boot/grub/grub.cfg
```

## Рекомендуемые темы GRUB

Можно использовать готовые темы в стиле macOS:

1. **Vimix GRUB Theme**
   - https://github.com/vinceliuice/grub2-themes
   - Стиль macOS Big Sur/Monterey

2. **Tela GRUB Theme**
   - https://github.com/vinceliuice/grub2-themes
   - Минималистичный стиль

3. **Создать свою тему**
   - Используйте GRUB Theme Editor
   - Или отредактируйте theme.txt вручную

## Формат theme.txt

Базовая структура:

```
# Название темы
title-text: ""
desktop-color: "#1e1e2e"
desktop-image: "background.png"
terminal-font: "Terminus Regular 14"

+ boot_menu {
  left = 15%
  top = 30%
  width = 70%
  height = 50%
  item_color = "#cdd6f4"
  selected_item_color = "#f5c2e7"
  item_height = 32
  item_padding = 10
  item_spacing = 8
  icon_width = 32
  icon_height = 32
  item_icon_space = 10
}

+ label {
  top = 82%
  left = 0
  width = 100%
  height = 20
  text = "🍎 macOS Arch"
  color = "#cdd6f4"
  align = "center"
}
```

## Тестирование темы

После установки темы можно протестировать в виртуальной машине или на реальном железе.

## Примечания

- Тема применяется только после установки системы на диск
- В Live ISO GRUB использует стандартную тему
- Для изменения темы после установки отредактируйте `/etc/default/grub` и запустите `grub-mkconfig`
