# 🔨 Сборка macOS Arch ISO

## Способ 1: GitHub Actions (автоматически)

Просто сделайте push в репозиторий - GitHub Actions соберет ISO автоматически.

```bash
git add .
git commit -m "Update configuration"
git push
```

ISO будет доступен в:
- **Artifacts** (временно, 7 дней)
- **Releases** (постоянно)

---

## Способ 2: Локальная сборка в виртуальной машине

### 📋 Требования

- **VirtualBox** или **VMware**
- **Arch Linux ISO**: https://archlinux.org/download/
- **RAM**: минимум 4 ГБ (рекомендуется 8 ГБ)
- **Диск**: минимум 30 ГБ
- **Интернет**: для установки пакетов

---

## 🖥️ Шаг 1: Установка Arch Linux в виртуалку

### 1.1 Создание виртуальной машины

**VirtualBox:**
```
Имя: ArchBuild
Тип: Linux
Версия: Arch Linux (64-bit)
RAM: 4096 МБ (или больше)
Диск: 30 ГБ (динамический)
```

**Настройки:**
- System → Processor → 2+ CPU
- System → Enable EFI
- Network → NAT или Bridged

### 1.2 Загрузка Arch Linux ISO

1. Скачайте: https://archlinux.org/download/
2. Подключите ISO к виртуалке
3. Загрузитесь с ISO

### 1.3 Установка базовой системы

**Вариант 1: Автоматическая установка (рекомендуется)**

```bash
# Скачайте скрипт
curl -O https://raw.githubusercontent.com/320kgpenguin/macos-arch/main/scripts/auto-install-arch.sh

# Запустите
bash auto-install-arch.sh
# Введите: yes

# Ждите ~10 минут
# Перезагрузитесь: reboot
```

**Вариант 2: Ручная установка**

```bash
# Проверка интернета
ping -c 3 archlinux.org

# Синхронизация времени
timedatectl set-ntp true

# Разметка диска (для UEFI)
cfdisk /dev/sda
# Создайте:
# /dev/sda1 - 512M - EFI System
# /dev/sda2 - остальное - Linux filesystem

# Форматирование
mkfs.fat -F32 /dev/sda1
mkfs.ext4 /dev/sda2

# Монтирование
mount /dev/sda2 /mnt
mkdir -p /mnt/boot
mount /dev/sda1 /mnt/boot

# Установка базовой системы
pacstrap /mnt base base-devel linux linux-firmware

# Генерация fstab
genfstab -U /mnt >> /mnt/etc/fstab

# Chroot
arch-chroot /mnt

# Настройка времени
ln -sf /usr/share/zoneinfo/Europe/Moscow /etc/localtime
hwclock --systohc

# Локаль
echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen
echo "ru_RU.UTF-8 UTF-8" >> /etc/locale.gen
locale-gen
echo "LANG=en_US.UTF-8" > /etc/locale.conf

# Hostname
echo "archbuild" > /etc/hostname

# Hosts
cat >> /etc/hosts << EOF
127.0.0.1   localhost
::1         localhost
127.0.1.1   archbuild.localdomain archbuild
EOF

# Пароль root
passwd
# Введите: root

# Пользователь
useradd -m -G wheel -s /bin/bash builder
passwd builder
# Введите: builder

# Sudo
echo "%wheel ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

# Установка необходимых пакетов
pacman -S --noconfirm grub efibootmgr networkmanager git vim nano sudo

# GRUB
grub-install --target=x86_64-efi --efi-directory=/boot --bootloader-id=GRUB
grub-mkconfig -o /boot/grub/grub.cfg

# Включение NetworkManager
systemctl enable NetworkManager

# Выход и перезагрузка
exit
umount -R /mnt
reboot
```

### 1.4 После перезагрузки

Войдите под пользователем `builder` (пароль: `builder`)

```bash
# Проверка интернета
ping -c 3 google.com

# Обновление системы
sudo pacman -Syu
```

---

## 🔨 Шаг 2: Сборка ISO

### 2.1 Установка зависимостей

```bash
# Установка archiso
sudo pacman -S --noconfirm archiso git

# Клонирование репозитория
cd ~
git clone https://github.com/320kgpenguin/macos-arch.git
cd macos-arch
```

### 2.2 Сборка ISO

```bash
cd build
sudo bash build.sh
```

**Процесс сборки:**
- Копирование профиля
- Установка пакетов (~10-20 минут)
- Кастомизация системы
- Создание ISO (~5-10 минут)

**Общее время:** ~20-40 минут (зависит от интернета)

### 2.3 Результат

ISO будет создан в `out/`:
```bash
ls -lh ../out/
# macos-arch-2026.01.19-x86_64.iso (~3-4 ГБ)
```

---

## 📤 Шаг 3: Копирование ISO на хост

### Способ 1: Общая папка (VirtualBox)

**На хосте (Windows):**
1. VirtualBox → Настройки VM → Общие папки
2. Добавить папку: `C:\Users\user\Downloads` → `/mnt/shared`
3. Автоподключение: ✓

**В виртуалке:**
```bash
# Установка Guest Additions (если нужно)
sudo pacman -S virtualbox-guest-utils
sudo systemctl enable vboxservice

# Копирование ISO
sudo mkdir -p /mnt/shared
sudo mount -t vboxsf shared /mnt/shared
sudo cp ~/macos-liquid-arch/out/*.iso /mnt/shared/
```

### Способ 2: SCP (через сеть)

**В виртуалке:**
```bash
# Установка SSH
sudo pacman -S openssh
sudo systemctl start sshd

# Узнать IP
ip addr show
# Например: 192.168.1.100
```

**На хосте (Windows PowerShell):**
```powershell
# Установите OpenSSH или используйте WinSCP
scp builder@192.168.1.100:~/macos-liquid-arch/out/*.iso C:\Users\user\Downloads\
```

### Способ 3: HTTP сервер

**В виртуалке:**
```bash
cd ~/macos-liquid-arch/out
python -m http.server 8000
```

**На хосте:**
Откройте браузер: `http://192.168.1.100:8000`
Скачайте ISO

---

## 🚀 Шаг 4: Тестирование ISO

### В той же виртуалке

```bash
# Создайте новую VM для тестирования
# Подключите собранный ISO
# Загрузитесь и проверьте
```

### Или на реальном железе

```bash
# Запишите на USB (Linux/macOS)
sudo dd bs=4M if=macos-arch-*.iso of=/dev/sdX status=progress oflag=sync

# Или используйте Rufus/Etcher на Windows
```

---

## 🔧 Полезные команды

### Очистка перед новой сборкой

```bash
cd ~/macos-liquid-arch/build
sudo rm -rf work/ out/
```

### Обновление репозитория

```bash
cd ~/macos-liquid-arch
git pull
```

### Изменение конфигурации

```bash
# Редактирование пакетов
nano packages.x86_64

# Редактирование скрипта кастомизации
nano airootfs/root/customize_airootfs.sh

# После изменений - пересборка
cd build
sudo bash build.sh
```

### Логи сборки

```bash
# Если сборка упала, смотрите логи
sudo journalctl -xe
```

---

## ❓ Решение проблем

### Не хватает места на диске

```bash
# Очистка кэша pacman
sudo pacman -Scc

# Увеличьте размер диска виртуалки
# VirtualBox: Файл → Менеджер виртуальных носителей
```

### Ошибки при установке пакетов

```bash
# Обновите зеркала
sudo pacman -Syy

# Обновите ключи
sudo pacman -S archlinux-keyring
```

### Медленная сборка

```bash
# Увеличьте RAM и CPU виртуалки
# VirtualBox: Настройки → Система
```

---

## 📊 Сравнение способов сборки

| Способ | Время | Сложность | Контроль |
|--------|-------|-----------|----------|
| GitHub Actions | ~40 мин | Легко | Низкий |
| Локальная VM | ~30 мин | Средне | Высокий |
| Реальный Arch | ~20 мин | Сложно | Полный |

---

## 💡 Советы

1. **Снимок VM** - сделайте snapshot после установки Arch, чтобы быстро откатиться
2. **Скрипт автоматизации** - создайте скрипт для повторяющихся сборок
3. **Кэширование** - сохраните `/var/cache/pacman/pkg/` между сборками
4. **Параллельная сборка** - используйте несколько VM для разных конфигураций

---

## 🎯 Быстрый старт (копипаста)

```bash
# После установки Arch в VM:
sudo pacman -S --noconfirm archiso git
git clone https://github.com/320kgpenguin/macos-liquid-arch.git
cd macos-liquid-arch/build
sudo bash build.sh
# Ждите ~30 минут
# ISO в ../out/
```

---

**Готово! Теперь у вас есть собственная сборочная машина для 320kgpenguin! 🐧🍎**
